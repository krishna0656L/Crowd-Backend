# Core web framework
flask==2.3.3
flask-cors==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# Web Server
gunicorn==21.2.0

# Data Processing (Python 3.13 compatible)
numpy>=2.1.2,<3
scikit-learn>=1.5.1,<2

# Image Processing (OpenCV >=4.10 is compatible with NumPy 2)
opencv-python-headless>=4.10.0,<5
# Pillow will be provided by torchvision if needed; keep a compatible upper bound
pillow>=11.0.0,<12

# PyTorch will be installed separately in build command
